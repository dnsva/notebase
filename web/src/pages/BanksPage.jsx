// ============================================================================
// notebase web — pages/BanksPage.jsx
// ============================================================================
// Question banks. PLACEHOLDER: built in Stage 2 Phase 5 (SPEC.md §13) —
// folder grid, bank study view, GitHub-backed editing. This stub exists so
// the navigation and routing land in Phase 4 and the section grows in place.
// ============================================================================

export default function BanksPage() {
  return (
    <div className="page">
      <h2 className="page-title">Question banks</h2>
      <p className="empty">Coming in Phase 5 — question banks with rich text, math, images, tags, and difficulty levels, editable right here.</p>
    </div>
  );
}
