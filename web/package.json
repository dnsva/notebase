{
  "name": "notebase-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static search UI for notebase. predev/prebuild copy ../pdfs and ../search-index.json into public/ so one source of truth lives in the repo root.",
  "scripts": {
    "predev": "node scripts/copy-assets.js",
    "dev": "vite",
    "prebuild": "node scripts/copy-assets.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0"
  }
}
